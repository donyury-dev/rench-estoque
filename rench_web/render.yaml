build:
  command: pip install -r requirements.txt

start:
  command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2

envVars:
  - key: SECRET_KEY
    generateValue: true
  - key: RENDER
    value: "1"
  - key: DATABASE_URL
    sync: false